{
  "name": "portfolio-2018",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://javascripterika.github.io/",
  "dependencies": {
    "font-awesome": "^4.7.0",
    "gh-pages": "^1.1.0",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-p5-wrapper": "^0.0.4",
    "react-reveal": "^1.2.2",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "yarn sass:build && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -b master -d build",
    "eject": "react-scripts eject",
    "sass": "sass --watch src/styles/sass:src/styles/css",
    "sass:build": "sass --update src/styles/sass:src/styles/css"
  }
}